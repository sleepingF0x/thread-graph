name: test

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  backend:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: threadgraph
          POSTGRES_PASSWORD: threadgraph
          POSTGRES_DB: threadgraph_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://threadgraph:threadgraph@localhost:5432/threadgraph_test
      QDRANT_HOST: localhost
      QDRANT_PORT: "6333"
      ANTHROPIC_API_KEY: sk-test-placeholder
      EMBEDDING_BASE_URL: http://localhost:11434/v1
      EMBEDDING_API_KEY: placeholder
      EMBEDDING_MODEL: nomic-embed-text
      TELEGRAM_API_ID: "12345"
      TELEGRAM_API_HASH: placeholder

    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Install dependencies
        run: uv pip install -r requirements.txt --system

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short

  frontend:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
